apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: elearning-frontend
          imagePullPolicy: Never   # IMPORTANT for Minikube local images
          ports:
            - containerPort: 3000
